package backend;

/** 0.7.3/1.0.4 兼容：倒计时阶段枚举（HScript 全局 Countdown）。 */
/** English: 0.7.3/1.0.4 compat — countdown phase enum (HScript global Countdown). */
enum Countdown
{
	THREE;
	TWO;
	ONE;
	GO;
	START;
}
